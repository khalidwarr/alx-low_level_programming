#include <stdio.h>
/**
 * main - Entry point
 * Description: 'Write a C program that prints, followed by a new line.'
 * Return: Always 0 (Success)
 */
int main(void)
{
int x = puts("\"Programming is like building a multilingual puzzle");
return (x);
}
